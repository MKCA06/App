import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const GatePassNavigator = () => {
  return (
    <View>
      <Text>GatePassNavigator</Text>
    </View>
  )
}

export default GatePassNavigator

const styles = StyleSheet.create({})